# app.py
import streamlit as st

st.set_page_config(
    page_title="都市救急システミックリスク・ダッシュボード",
    layout="wide",
)

st.title("🚑 都市救急 システミックリスク・ダッシュボード")
st.markdown("### QUBO × 救急医療 × 相関ネットワークによる次世代アナリティクス")

st.markdown(
    """
このアプリは、**「どのゾーンが、どの病院に、どれだけ依存しているか」** を使って、

- ✅ **都市レベルの救急リスク地図（どこが危ないか）**
- ✅ **シナリオシミュレーション（○○が起きたらどう崩れるか）**
- ✅ **連鎖崩壊ネットワーク（どの病院が倒れると一気に連鎖するか）**

を、**対話的に & 一目で分かる形** に可視化します。

左のサイドバーからページを選んでください。
"""
)

st.markdown("---")

st.markdown(
    """
### 🧩 ページ構成

1. **📌 都市リスクマップ（静態）**  
   - メッシュごとの `risk_score` を地図上に表示  
   - どのエリアが構造的に危ないかを Overview で把握  

2. **🧪 シナリオシミュレーション**  
   - 「高リスクゾーンを1.5倍にしたら？」「上位10メッシュが急増したら？」など  
   - ビフォー / アフターのリスク地図と、**AIサマリー** を自動表示  

3. **🌐 連鎖崩壊ネットワーク**  
   - メッシュ × 病院の依存構造から、**病院同士の結びつき** を集計  
   - 「この病院が飛ぶと、どのくらい都市全体が揺れるか」を見るページ  
"""
)
